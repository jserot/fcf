let gcd (a:unsigned<16>, b:unsigned<16>) =
  let compute (m: unsigned<16>,n: unsigned<16>) = 
  | m>n -> compute (m-n,n)
  | m<n -> compute (m,n-m)
  | m=n -> return m in
  compute (a,b)
;

gcd(6,24);
gcd(13,17);
gcd(8,4);
